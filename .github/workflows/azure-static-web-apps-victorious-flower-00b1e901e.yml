name: Disabled duplicate Azure Static Web Apps workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled duplicate. Use .github/workflows/azure-static-web-apps.yml."
