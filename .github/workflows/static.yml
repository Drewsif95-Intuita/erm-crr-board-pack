name: Disabled GitHub Pages workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deployment is disabled. Use the Azure Static Web Apps workflow."
